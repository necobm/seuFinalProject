framework:
    test: true
    session:
        storage_id: session.storage.mock_file

swiftmailer:
    transport: "smtp"
    host:      "localhost"
    username:  ""
    password:  ""
    spool:     { type: memory }
